export abstract class Constants {

    //Número de elementos máximos del listado.
    static readonly maxElements = 1084;

    //Número de imágenes que se muestran en cada carga.
    static readonly displayedImages = 10;

    //Tipos de búsqueda.
    static readonly textSearchType = 'Texto';
    static readonly idSearchType = 'ID';
}